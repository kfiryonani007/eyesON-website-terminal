# EYESON — קובץ העברה (HANDOFF)

> עודכן: 22/09/2026 · נכתב מול הקוד בפועל, לא מהזיכרון.

## מצב נוכחי

**הכל בגיט והכל באוויר.** העבודה נדחפה ל-`main` בקומיט `237090c`
ו-Vercel פרסם אותה אוטומטית לפרודקשן (דיפלוי
`dpl_BNu4TRtmSsmnRBk11CfWJSivDjjc`, מצב READY).

אומת מול האתר החי: `header-slogan`, `a11y.js`, `footer-credit`,
`eyeson_israel`, `shrink-stats` נמצאים; `hero-logo` נעלם. כל הנכסים
החדשים מוגשים ב-200.

העבודה כבר לא קיימת בעותק אחד — במחשב אחר מספיק
`git clone https://github.com/kfiryonani007/eyesON-website-terminal.git`.

> ⚠️ **המספרים בגרף הם נתוני הדגמה והם כרגע חיים באוויר.**
> הם מסומנים "נתוני הדגמה בלבד" על המסך, אבל יש להחליף
> אותם בנתונים אמיתיים. ראו סעיף 6.

---

## 1. מה הפרויקט

EYESON היא חברה ישראלית לניטור מצלמות אבטחה מרחוק — חמ"ל מאויש 24/7 שצופה
במצלמות של עסקים, מזהה גניבות ואירועים חריגים ומדווח ישירות לנייד (לא התראות
AI אוטומטיות). זהו אתר תדמית מלא בעברית/RTL, **פרויקט לקוח אמיתי ולא דמו**,
בנוי כ-HTML + CSS + JS סטטיים בלבד — ללא framework וללא תהליך build, כדי
שאפשר יהיה לארח אותו בכל מקום.

---

## 2. קישורים חיים

נלקחו מ-`.claude/launch.json`, מ-API של Vercel ומהקוד עצמו.

| מה | כתובת |
|---|---|
| **פרודקשן** | https://eyes-on-website-terminal.vercel.app/ |
| דיפלוי ישיר | `eyes-on-website-terminal-4l1jq4kj2-kfir3.vercel.app` |
| כתובת הענף | `eyes-on-website-terminal-git-main-kfir3.vercel.app` |
| **גיט** | https://github.com/kfiryonani007/eyesON-website-terminal |
| **לוח בקרה** | https://vercel.com/kfir3/eyes-on-website-terminal |
| בדיקה מקומית | http://localhost:3100 |
| אתר החברה (חיצוני) | https://eyeson.co.il |

**צוות Vercel:** `kfir3` (`team_xbRDuB1zWiQPzByHGMwVwdod`)
**מזהה פרויקט:** `prj_IJzrVMNewKGmQhCDIlXMsIzCtYmj`

### ⚠️ פרויקטים ישנים — להתעלם מהם

בחשבון Vercel יש עוד כמה פרויקטי EYESON שהם builds ישנים. אל תערכו אותם
ואל תשוו מולם:

- `eyeson-a` · `eyeson-yoal-kfir-the-king` — קרובים אבל מיושנים
- `website-2029-gif-hiro` — **ישן במיוחד**: חסרים בו העמודים המשפטיים
  וסרגל הניסיון

**איך לזהות שאתם על הגרסה הנכונה:** בסרגל האדום העליון כתוב
`🎯 תקופת ניסיון מוגבלת ·` בלי המילים "ללא כרטיס אשראי". אם רואים את
התוספת הזו — זה build אחר.

---

## 3. מבנה

הקוד יושב ישירות בשורש `C:\Users\user\Desktop\eyesON` — אין `src/`, אין
`package.json`, אין תלויות. **ענף: `main`** (בריפו המרוחק; מקומית אין גיט).

```
*.html                    8 עמודים
css/styles.css            כל העיצוב (~46KB) — משתני מותג בראש הקובץ
js/main.js                גלילת הירו, מונים, תפריט מובייל, כרטיסי הטיה, טופס
js/a11y.js                תפריט נגישות (קובץ חדש)
assets/hero/frames/       30 פריימים, frame-001..030.jpg (900×506)
assets/images/            9 תמונות (חלקן אמיתיות מהלקוח)
assets/logos/             9 גרסאות לוגו SVG
assets/kfir-ai-logo.png   לוגו הקרדיט בפוטר
.claude/launch.json       הגדרת שרת הפיתוח (פורט 3100)
```

**8 העמודים:** `index` · `service` · `screen` · `clients` · `contact` ·
`privacy` · `terms` · `accessibility`

---

## 4. מה כבר בנוי ועובד

- **8 עמודים מלאים** בעברית/RTL, הדר ופוטר אחידים.
- **הירו עם גלילת scrub** — 30 פריימים מסרטון הדגמה. הסקשן נעצר במקום
  (`position:sticky`) והגלילה מריצה את הפריימים. לא מתנגן לבד.
- **תפריט נגישות מלא** (`js/a11y.js`) בכל 8 העמודים: גודל טקסט, ניגודיות
  גבוהה, היפוך צבעים, גווני אפור, הדגשת קישורים, גופן קריא, ריווח שורות,
  עצירת אנימציות, סמן עכבר גדול, איפוס. בנוסף: קישור "דילוג לתוכן" ומצב
  פוקוס נראה למקלדת. ההעדפות נשמרות ב-`localStorage` תחת `eyeson-a11y`.
  נבדק: הפעלה, שמירה, איפוס, והדדיות בין ניגודיות להיפוך — הכל עובד.
- **סקשן סטטיסטיקה + גרף** (`#problem-teaser` ב-`index.html`) — שלושה
  נתוני מפתח וגרף עמודות "לפני/אחרי EYESON". ⚠️ המספרים הם הדגמה, ראו סעיף 6.
- **קישורים חברתיים אמיתיים** — אומת בקוד, 5 מופעים לכל אחד:
  - אינסטגרם: `https://www.instagram.com/eyeson_israel`
  - טיקטוק: `https://www.tiktok.com/@eyeson_israel`
- **קרדיט בפוטר** בכל 8 העמודים — לוגו + "נבנה על ידי Kfir-ai.com"
  שמקשר ל-`https://kfir-ai.com`.
- **פרטי קשר אמיתיים:** טלפון/וואטסאפ `055-2622272`,
  מייל `info@eyeson.co.il`.

---

## 5. החלטות שקיבלנו ולמה

**בלי framework, בלי build.** גם כשנוספו אפקטים מורכבים (הטיה תלת-ממדית
בכרטיסים, גלילת scrub) נשארנו ב-JS רגיל. הסיבה: אחסון סטטי בכל מקום, בלי
תהליך פריסה.

**העותק המקומי נמשך מהדיפלוי, לא מדיסק און קי.** הקוד שוחזר משיבוט של
ריפו הגיטהאב. לכן כל מה שמעולם לא נדחף לגיטהאב פשוט לא קיים כאן.

**העינית (`.eye-cursor`) הוסרה לבקשת המשתמש.** תוך כדי המחיקה התגלה ש-
`finePointer` הוגדר *בתוך* הבלוק שנמחק ומשמש גם את כרטיסי ההטיה — הוא הוגדר
מחדש לפני בלוק `tilt cards`. אם מוחקים עוד קוד באזור הזה, לבדוק את זה שוב.

**גלילת ההירו נכתבה מחדש.** הקוד הישן החליף `img.src` בכל פריים, מה שמאלץ
פענוח מחדש של התמונה וגרם לקפיצות. עכשיו כל 30 הפריימים מורכבים מראש
כשכבות `<img>` נפרדות ומתחלפת רק ה-opacity, עם החלקה (`pos += diff * 0.3`).
אורך ה-pin קוצר מ-350vh ל-**190vh** (ו-140vh במסכים קטנים) כדי שהרצף
יתקדם מהר יותר. פריים 1 הוא שכבת הבסיס ותמיד נצבע — כך האתר תקין גם בלי
JS וגם ב-`prefers-reduced-motion`.

**תוויות המספרים בגרף מוקמו absolute.** כשהן היו חלק מזרימת ה-flex הן
גזלו מקום והעמודות הגבוהות נחתכו כולן לאותו גובה. כרגע יש `padding-top`
בתוך `.chart-bars` כדי שהתוויות לא יתנגשו בכותרת.

**הקו המפריד (em dash) הוסר מכל הטקסט הגלוי.** לא נמחק אלא הוחלף בפיסוק
לפי הקשר: פסיק באמצע משפט, נקודתיים לפני פירוט, קו אנכי בכותרות `<title>`,
ובציטוטים הוסר לגמרי. מחיקה עיוורת הייתה שוברת משפטים. נותרו כ-29 מופעים
בהערות קוד בלבד (HTML/CSS/JS) שאינן נראות למבקרים. **אם מוסיפים טקסט חדש
לאתר, לא להשתמש ב-—.**

**הפער בין ההדר להירו בגלילה תוקן.** ההדר מתכווץ מ-124px ל-92px בגלילה,
אבל ההירו נדבק ב-`top: var(--header-h)` = 124px, כך שנפתח פס של רקע השורש
בגובה 32px בין ההדר לתמונה. נוסף משתנה `--header-h-compact: 92px` וההירו
נדבק אליו. בנוסף, מעבר הגובה של ההדר קוצר מ-0.35s ל-0.15s — אחרת בגלילה
מהירה ההירו נדבק לפני שההדר סיים להתכווץ ונכנס לרגע מתחתיו.

**פרמטרי מעקב הוסרו מקישור הטיקטוק.** הקישור שהתקבל הכיל `?_r=1&_t=...`
— פרמטרים שנוצרים בשיתוף מהאפליקציה. בקוד נשמרת כתובת הפרופיל הנקייה.

### באגים שכבר תוקנו בעבר (שווה להכיר)

1. `overflow-x: hidden` על `<body>` אילץ `overflow-y: auto` — תוקן ל-`clip`.
2. תוויות טופס לבנות על רקע לבן בסקשן כהה — תוקן בסטיילינג ייעודי.
3. חץ ה-`select` שוכפל להרבה משולשים — בגלל `background:` (קיצור דרך)
   שאיפס בשקט את `background-repeat`. תוקן ל-`background-color:`.
4. Parallax שנתקע על 0,0 — שילוב `inset:0; margin:auto` עם `translate()`
   באחוזים. תוקן למירכוז `top/left:50%` + `translate(-50%,-50%)`.

---

## 6. מה פתוח

### חוסם פרסום

- **המספרים בגרף הם המצאה.** `--h` ו-`chart-val` ב-`index.html` מכילים
  8,400 / 9,100 / 8,700 (לפני) ו-3,200 / 1,900 / 1,400 (אחרי). הם מסומנים
  "נתוני הדגמה בלבד" על המסך ובהערת `TODO(client)` בקוד. **אסור לפרסם
  סכומים שאי אפשר לגבות** באתר שמוכר שירות.

### פתוח

- **הפוטר לא אחיד** — ל-`privacy.html`, `terms.html`, `accessibility.html`
  אין בכלל אייקונים חברתיים (`footer-social` = 0). היה ככה מלכתחילה.
  נשאלה שאלה אם להוסיף, טרם התקבלה תשובה.
- **הצהרת נגישות חסרה פרטים** — חסרים שם רכז/ת נגישות ודרכי פנייה.
  התפריט הוא התאמה טכנית בלבד; החוק בישראל דורש גם בדיקה של מורשה
  נגישות שירות. **אין כאן ייעוץ משפטי.**
- **עמודים משפטיים הם שלד** — `privacy.html` ו-`terms.html` חייבים
  עורך/ת דין.
- **פריימי ההירו ברזולוציה נמוכה** — 900×506 בלבד למסך מלא, ולכן זה
  נראה רך. יש פקודת ffmpeg מוכנה ב-`README.md` ליצירת רצף חדש.
- **וידאו אמיתי למקרה הבוחן** ב-`clients.html` — כרגע placeholder.
- **הטופס עובד דרך `mailto:` בלבד** — אין endpoint אמיתי. הוא פותח את
  תוכנת המייל של המשתמש עם הפרטים ממולאים.
- **תמונת OG** היא SVG — מומלץ PNG/JPG 1200×630.
- **אין דומיין משלו** — רק כתובת `.vercel.app`.

### ידוע ולא באג

צילומי מסך מחלונית התצוגה יוצאים שחורים אחרי גלילה. נבדק מול האתר החי
הלא-ערוך — קורה גם שם. זו מגבלה של החלונית, לא של הקוד.

---

## 7. הצעד הבא המומלץ

1. **להחליף את מספרי הגרף בנתונים אמיתיים** — זה הדבר היחיד
   באתר שמוצג למבקרים ואינו נכון. הערכים ב-`index.html`,
   תחת `--h` ו-`chart-val`.
2. **להשלים את הצהרת הנגישות** — שם רכז/ת נגישות ודרכי
   פנייה. זו חובה חוקית בישראל והאתר כבר חי.
3. להעביר את `privacy.html` ו-`terms.html` לעורך/ת דין.
4. לחבר דומיין אמיתי במקום כתובת `.vercel.app`.

**איך מפרסמים שינוי מעכשיו:** `git push origin main` — Vercel בונה
ומפרסם לפרודקשן אוטומטית. אין שלב ביניים ואין אישור —
כל דחיפה ל-`main` עולה ישירות לאוויר.

## 8. איך מריצים

**התקנה:** אין. אין `package.json`, אין תלויות, אין תהליך build.

**הרצה מקומית:**

```bash
npx serve .
```

או דרך הגדרת ה-preview שכבר קיימת ב-`.claude/launch.json` (שם: `eyeson`,
פורט **3100**). הפורט הוא 3100 ולא 3000 כי 3000 תפוס על ידי שרת של
פרויקט `kfir-ai-site`. אפשר גם פשוט לפתוח את `index.html` בדפדפן.

**העלאה לאוויר:** דחיפה לענף `main` בריפו מפרסמת אוטומטית ב-Vercel
(GitHub integration). אין פקודת deploy ידנית.

---

## 9. משתני סביבה

**אין.** אומת בקוד — אין שום התייחסות ל-`process.env`, `import.meta.env`,
`API_KEY`, `SECRET`, `TOKEN` או קובץ `.env`. זהו אתר סטטי לחלוטין; טופס
הקשר עובד דרך `mailto:` ולא מול שרת, ולכן אין מפתחות או סודות כלשהם.

אם בעתיד יחובר endpoint אמיתי לטופס — שם המשתנה יוגדר בלוח הבקרה של
Vercel, לא בקוד.
