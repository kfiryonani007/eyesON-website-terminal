# EYESON — קובץ המשך עבודה (למחשב אחר)

קובץ זה נועד לתת לקלוד קוד (בכל מחשב) את מלוא ההקשר על הפרויקט, כדי שתוכל
להמשיך לעבוד עליו בלי לאבד כלום מההיסטוריה. **הקובץ הזה לבד לא מספיק** —
הוא רק תיעוד. אתה צריך להעביר גם את **תיקיית הפרויקט המלאה**.

---

## ⚠️ שלב ראשון: להעביר את התיקייה למחשב הבית

הפרויקט נמצא ב:
```
C:\Users\Callisto\Desktop\EYESON
```

אין לו עדיין קישור אינטרנט — **הוא לא מפורסם באין** (לא Vercel, לא Netlify,
כלום). זה אתר סטטי שרץ רק מקומית עד שתעלה אותו. לכן חובה להעביר את **כל
התיקייה**, לא רק את הקובץ הזה. הדרך הכי פשוטה:

1. **דיסק און קי / כונן חיצוני** — להעתיק את כל תיקיית `EYESON` (כולל
   `assets/`, `css/`, `js/`, כל קבצי ה-`.html`) — הכי פשוט ובטוח.
2. **Google Drive / OneDrive** — להעלות את התיקייה לענן מהמחשב הזה, להוריד
   בבית. שימו לב שתיקיית `assets/images` ו-`assets/hero/frames` מכילות הרבה
   קבצי תמונה — יכול לקחת כמה דקות להעלות/להוריד.
3. **Git/GitHub** (הכי מומלץ לטווח ארוך) — אם תרצה, בפעם הבאה שנדבר אני יכול
   להקים ריפו git ולדחוף אותו ל-GitHub, ואז מכל מחשב פשוט `git clone`.
   לא עשינו את זה עדיין בפרויקט הזה.

**בבית:** לפתוח את קלוד קוד בתוך תיקיית `EYESON` שהעברת, ואז להגיד לו
(או להדביק) — "קרא את HANDOFF.md כדי להבין את ההקשר של הפרויקט".

---

## על הפרויקט

**EYESON** — חברה ישראלית לניטור מצלמות אבטחה מרחוק (חמ"ל אנושי, לא רק AI).
בונים אתר תדמית מלא בעברית/RTL. זה **פרויקט לקוח אמיתי**, לא דמו.

- **סטאק:** HTML + CSS + JS פשוטים, בלי framework ובלי build process (בכוונה
  — קל לארח בכל מקום). ראו `README.md` לפרטים טכניים מלאים (מבנה קבצים,
  הרצה מקומית, TODO-ים ללקוח).
- **שפה:** עברית מלאה, RTL (`dir="rtl" lang="he"`).
- **8 עמודים:** `index.html` (בית), `service.html` (השירות), `screen.html`
  (EyesOn Screen), `clients.html` (לקוחות), `contact.html` (צור קשר),
  ועמודים משפטיים: `privacy.html`, `terms.html`, `accessibility.html`.

### עובדות אמיתיות שנאספו מהאתר החי (eyeson.co.il)
- טלפון/וואטסאפ: **055-2622272**
- מייל: **info@eyeson.co.il**
- שעות פעילות: א׳–ה׳ 08:00–20:00, ו׳–שבת 09:00–18:00 (חמ"ל עצמו פעיל 24/7)
- תקופת ניסיון: מוגבלת, ללא כרטיס אשראי, התקנה תוך 24 שעות (הניסוח באתר
  שונה בכוונה מ"30 יום" ל**"תקופת ניסיון מוגבלת"** לפי בקשת הלקוח)

---

## מה כבר נבנה — סיכום מלא

### מבנה ותוכן
- 8 עמודים מלאים לפי המבנה למעלה
- קישור "איך זה עובד" נוסף לתפריט הראשי בכל העמודים
- טופס יצירת קשר מלא (שם, טלפון, מייל, שם עסק, סוג עסק, כמות מצלמות, הודעה)
  משולב גם בעמוד הבית וגם בעמוד "צור קשר" — עובד כרגע דרך `mailto:` (ראו
  TODO ב-README לגבי חיבור endpoint אמיתי)
- בעמוד הבית: "לקוחות מספרים" הוזז מהמקום הראשון, הוחלף בטיזר קומפקטי;
  הועברו סטטיסטיקות (+6,000 אירועים, +1,500 מצלמות, 24/7) לסקשן "מי אנחנו"

### ההירו (index.html)
- **לא GIF, לא וידאו רגיל** — רצף של 30 פריימים שחולצו מסרטון הדגמה אמיתי
  (`assets/hero/frames/frame-001.jpg` עד `frame-030.jpg`)
- ה"סרטון" **לא מתנגן לבד**: הסקשן נעצר במקום (CSS `position:sticky`
  בתוך wrapper גבוה), והגלילה של המשתמש היא שמריצה את הפריימים. כשמגיעים
  לפריים האחרון, הגלילה משתחררת וממשיכה כרגיל באתר. הלוגיקה ב-`js/main.js`
  תחת ההערה "hero scroll scrub".

### עיצוב וברנד'ינג
- מערכת צבע אדום (גרדיאנט + זוהר + טקסט גרדיאנטי), לא צבע שטוח
- לוגו בהדר מוגדל משמעותית, תפריט ממורכז (grid 3 עמודות)
- במובייל: לוגו בצד שמאל, כפתור המבורגר בימין (תקין ל-RTL)
- **עינית עכבר מותאמת אישית** — עדשה אדומה זוהרת שרודפת אחרי הסמן ומחליפה
  את סמן העכבר הרגיל לגמרי (`cursor: none` גלובלי כשהיא פעילה). פעיל רק
  בדסקטופ עם עכבר אמיתי, מכובה אוטומטית ב-`prefers-reduced-motion`
  ובמגע/מובייל.
- סרגל "תקופת ניסיון מוגבלת — ללא כרטיס אשראי" קבוע בראש כל עמוד

### כרטיסי "מי אנחנו" (index.html)
- הפכו מ-3 עמודות צרות לכרטיסים אופקיים ברוחב מלא
- האייקונים הוחלפו בסמל המותג של EYESON (`assets/favicon.svg`)
- אפקט הטיה תלת-ממדית + זוהר עוקב עכבר — **JS רגיל, בלי React** (הוחלט
  במפורש להישאר עם הסטאק הפשוט)
- לוגו-רקע גדול שזז/עוקב אחרי העכבר על פני כל הכרטיס בריחוף

### תמונות
כמה תמונות סטוק שהתבררו כמטעות (מיצג אמנות שנראה כמו חדר בקרה, חדר מכונות
אונייה, תחנת כוח ישנה) הוסרו והוחלפו. שולשה תמונות אמיתיות **מהלקוח עצמו**
(לא סטוק): תמונת צוות בעמדות עבודה, מצלמה עילית מעל הקופה, וצילום מסך אמיתי
של EyesOn Screen (קופה+מצלמה זה לצד זה). פירוט מלא בטבלה ב-`README.md`.

### פוטר
- פרטי קשר אמיתיים (טלפון/מייל/שעות) בכל עמוד
- לוגו הפוטר **מקושר לעמוד הבית** (`index.html`) — קודם היה רק תמונה לא-לחיצה
- קישורים לעמודים המשפטיים
- ריווח תחתון נוסף במובייל (`padding-bottom: 5.5rem`) כדי שכפתור הוואטסאפ
  הצף לא יכסה את השורה האחרונה

### סקשן חדש ב-`service.html`: "זיהוי אוטומטי מסמן. אימות אנושי קובע."
סקשן עומק על ההבדל בין מערכת אנליטיקה אוטומטית לצופה אנושי ייעודי — כותרת,
פסקת הסבר, 3 כרטיסים (הערכת כוונה והקשר · ראייה רציפה של האירוע · אחריות
אישית על האתר), ושורת סגירה מודגשת. ממוקם בין טבלת ההשוואה ל"איך זה עובד".
הטקסט המלא סופק ע"י הלקוח.

### תיקוני מובייל (סבב אחרון — מבוסס על צילומי מסך אמיתיים מהלקוח)
- **כפתור סגירת התפריט (X) נעלם** — תוקן (ראו "באגים אמיתיים" למטה)
- **3 הצ'יפים בהירו** ("ללא כרטיס אשראי" וכו') עכשיו נשארים בשורה אחת
  במובייל (עם גלילה אופקית כגיבוי), וה"24/7" בפסקת ההירו מוצמד ברווח
  בלתי-שביר כדי שלא ייקרע לשורה נפרדת
- ה-badge "כל לקוח מקבל צפייה אישית ב-24 שעות" **הוסר** מסקשן "מי אנחנו"
  (הלקוח לא הבין את המשמעות שלו)
- סדר 3 כרטיסי "מי אנחנו" הוחלף (הראשון עבר לסוף), והטקסטים נכתבו מחדש
  לבהירות
- "תקופת ניסיון מוגבלת" הוסר מהמשפט בסקשן "איך זה עובד" בעמוד הבית
- כפתור ה-CTA הסופי ב-`screen.html` שונה מ"התחילו תקופת ניסיון מוגבלת"
  ל**"לרכישת EyesOn Screen"** — ל-Screen אין ניסיון חינם, זה תוסף בתשלום
  (יש רמז לזה כבר ב-fine-print: "ניתן לרכוש במנוי חודשי/שנתי")
- תמונת הרקע בטיזר EyesOn Screen (עמוד הבית) הוחלפה מציוד מצלמות לתמונת
  עמדת קופה/תשלום (`pos-terminal.jpg`) — מתאימה יותר לנושא

---

## באגים אמיתיים שנתקלנו בהם ותוקנו (שווה לדעת אם רואים משהו דומה)

1. **`overflow-x: hidden` על `<body>`** גרם בטעות ל-`overflow-y: auto`
   (התנהגות ספק ב-CSS: overflow-x שאינו visible "מכריח" overflow-y ל-auto
   אם לא הוגדר) — תוקן ל-`overflow-x: clip`.
2. **תוויות טופס לבנות על רקע לבן** בסקשן כהה — היו בלתי נראות. תוקן עם
   סטיילינג כהה ייעודי לטופס כשהוא בתוך `.section-dark`.
3. **חצי ה-select הפכו למספר משולשים חופפים** — הסיבה: כלל CSS השתמש ב-
   `background: X` (קיצור-דרך) במקום `background-color: X`, מה שאיפס
   בשקט את `background-repeat` בחזרה ל-`repeat` ברירת המחדל, וגרם ל-SVG
   הקטן של החץ לרצף את עצמו. תוקן בשלושה מקומות (מנוחה/פוקוס, בהיר/כהה).
4. **תזוזת parallax שנתקעה על 0,0** — שילוב של `inset:0; margin:auto`
   (למירכוז) יחד עם `translate()` מבוסס-אחוזים בתוך `calc()` איפס את
   התזוזה בשקט בכרום. תוקן במעבר לשיטת מירכוז `top:50%;left:50%` +
   `translate(-50%,-50%)`.
5. **כפתור ה-X לסגירת התפריט במובייל "נעלם"** — הפאנל הפתוח של `.main-nav`
   הוא `position:fixed` בלי `z-index` משלו; אלמנטים ממוקמים (positioned)
   מציירים את עצמם מעל אלמנטים סטטיים כברירת מחדל, גם אם הם מוקדמים יותר
   ב-DOM — כך שהפאנל (עם הרקע השחור האטום) כיסה את כפתור ההמבורגר/X
   שמתחתיו. תוקן עם `.nav-toggle { position:relative; z-index:101; }`.
   אומת בבדיקת פיקסל: `document.elementFromPoint` על מרכז הכפתור מחזיר
   את הכפתור עצמו, לא את הפאנל.
6. **הפוטר "נעלם" במובייל** — לא באג CSS אמיתי אלא כפתור הוואטסאפ הצף
   (`position:fixed`, פינה שמאלית-תחתונה) שיושב בדיוק על השורה האחרונה של
   הפוטר כשמגיעים לסוף הדף. תוקן עם ריווח תחתון נוסף בפוטר במובייל.

---

## TODO-ים פתוחים (גם ב-README.md, עם הערות `TODO(client)` בקוד עצמו)

1. להחליף את פריימי ההירו בצילום אמיתי מהעסק (יש פקודת ffmpeg מוכנה ב-README)
2. וידאו אמיתי למקרה הבוחן ב-`clients.html` (כרגע placeholder)
3. להחליף שאר תמונות הסטוק בצילומים אמיתיים
4. לחבר endpoint אמיתי לטופס יצירת הקשר (כרגע `mailto:` בלבד)
5. לעדכן קישורי רשתות חברתיות בפוטר (כרגע גנריים)
6. **עמודים משפטיים הם שלד בלבד — לא ייעוץ משפטי, חובה עורך/ת דין**
   (הצהרת נגישות היא חובה חוקית בישראל)
7. תמונת OG (שיתוף ברשתות) — כרגע SVG, מומלץ PNG/JPG 1200×630
8. **האתר עדיין לא מפורסם באינטרנט** — צריך אירוח (Netlify/Vercel/Cloudflare
   Pages/cPanel) ודומיין

---

## איך להריץ מקומית

```bash
npx serve .
```
או פשוט לפתוח `index.html` בדפדפן.

---

*קובץ זה נוצר אוטומטית כדי לתעד את מצב הפרויקט לצורך המשך עבודה ממחשב אחר.
לא צריך לערוך אותו ידנית — אם משהו משתנה מהותית בפרויקט, כדאי לבקש מקלוד
לעדכן אותו.*
